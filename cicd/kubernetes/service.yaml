apiVersion: v1
kind: Service
metadata:
  name: production-devops-app-service
spec:
  type: NodePort
  selector:
    app: production-devops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
